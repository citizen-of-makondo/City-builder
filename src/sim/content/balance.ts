/**
 * Все числа баланса — только здесь. Системы берут коэффициенты отсюда,
 * не хардкодят их у себя.
 */
export const balance = {
  /** КПД оффлайн-прогресса (PLAN.md 4.3) */
  offlineEfficiency: 0.5,
  /** максимум тиков оффлайн-прогресса за одну загрузку (24 часа) */
  offlineMaxTicks: 24 * 60 * 60,
  /** интервал автосейва, секунд */
  autosaveIntervalSec: 30,
} as const;

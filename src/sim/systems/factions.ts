import type { GameState } from '../state';
import type { SimEvent } from './events';

/** Фаза 5: фракции влияния, лояльность, эдикты. */
export function runFactions(state: GameState, _events: SimEvent[]): GameState {
  return state;
}

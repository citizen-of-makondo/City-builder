import type { GameState } from '../state';
import type { SimEvent } from './events';

/** Фаза 4: классы населения, потребности, счастье, мобильность. */
export function runEstates(state: GameState, _events: SimEvent[]): GameState {
  return state;
}

import type { GameState } from '../state';
import type { SimEvent } from './events';

/** Фаза 8: цепочка обучающих и повторяемые квесты. */
export function runQuests(state: GameState, _events: SimEvent[]): GameState {
  return state;
}

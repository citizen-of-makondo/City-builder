import type { GameState } from '../state';
import type { SimEvent } from './events';

/** Фаза 4: численность по типам жилья, рабочие места. */
export function runPopulation(state: GameState, _events: SimEvent[]): GameState {
  return state;
}

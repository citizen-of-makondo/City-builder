import type { GameState } from '../state';
import type { SimEvent } from './events';

/** Фаза 6: древо технологий, условия и церемония перехода эпохи. */
export function runEras(state: GameState, _events: SimEvent[]): GameState {
  return state;
}
